# Tests

- Ensure a local HotPocket node is running on ws://localhost:8081 with this contract deployed.
- Run tests from project root:

```
npm test
```

Tests perform a CRUD lifecycle on the Students service and assert expected responses.
